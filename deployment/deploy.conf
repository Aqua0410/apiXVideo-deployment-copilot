# =============================================================================
# FastAPI Video API - Deployment Configuration
# =============================================================================
# This file defines your deployment settings.
# Edit this ONCE, then you can redeploy to any server with a single command.
# Version control this file (don't include sensitive data in git)

# Project Configuration
DEPLOY_DIR="/var/www/fastapi-video-api"
SERVICE_NAME="fastapi-video-api"
INTERNAL_PORT="8000"
DOMAIN_NAME="videodownloaderyt.xyz"

# API Security (CHANGE THIS!)
SESSION_SECRET="jjF7qmKccGP+B7ie6syGuf7PgYvu1Vx7B2HYjq1TtMWECVniuGlqChWFfk5GJbrXMWCCxINesHJrWgXT8Uehog=="

# Worker Configuration
WORKER_MULTIPLIER="2"  # workers = CPU_count * WORKER_MULTIPLIER (default: 2)
MAX_REQUESTS="5000"
TIMEOUT="60"

# Git Configuration (optional - for auto-deployments)
GIT_REPO=""  # Leave empty if not using git
GIT_BRANCH="main"

# Logging
LOG_LEVEL="info"

# =============================================================================
# NOTES:
# - Adjust INTERNAL_PORT if you have conflicts (check: ss -tuln | grep :PORT)
# - DOMAIN_NAME is used for CyberPanel configuration documentation
# - For new deployments, keep GIT_REPO empty (manual git setup)
# =============================================================================
